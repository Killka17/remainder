services:

  db:
    image: postgres
    restart: always
    # set shared memory limit when using docker-compose
    shm_size: 128mb
    # or set shared memory limit when deploy via swarm stack
    #volumes:
    #  - type: tmpfs
    #    target: /dev/shm
    #    tmpfs:
    #      size: 134217728 # 128*2^20 bytes = 128Mb
    environment:
      POSTGRES_PASSWORD: 1234
      POSTGRES_USER: admin
      POSTGRES_DB: remainder
    ports:
      - 5432:5432

  backend:
    container_name: scala-backend
    build: .
    volumes:
      - ./remainder:/app/remainder
    restart: always
    environment:
      CHAT_ID: ???
      POSTGRES_DB: remainder
      POSTGRES_HOST: db
      POSTGRES_PASSWORD: 1234
      POSTGRES_PORT: 5432
      POSTGRES_USER: admin
      SERVER_HOST: 0.0.0.0
      SERVER_PORT: 8080
      TELEGRAM_URL: https://api.telegram.org/bot8087449989:AAHULcYMmUzGCe9GXkt5Rkxer_zJpFm0_r0/sendMessage
    ports:
      - 8080:8080
    

